module.exports = {
    test: (para) => {
        console.log("resulat", para + 1)
    },
    user: (name, age) => {
        console.log("Florentin: name: " + name + " age : " + age)
        console.log("Darcy: name: " + name + " age : " + age)
    }

}